frontend:
  baseUrl: http://localhost:3000

backend:
  baseUrl: http://localhost:7007
  database:
    connection:
      host: postgres
      user: postgres
      password: postgres
  cors:
    origin: '*'
    methods: [GET, POST, PUT, DELETE]
    credentials: true
