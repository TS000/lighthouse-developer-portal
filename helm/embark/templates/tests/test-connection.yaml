apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "embark.fullname" . }}-test-connection"
  labels:
    {{- include "embark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "embark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
